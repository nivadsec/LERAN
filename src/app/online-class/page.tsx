'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function OnlineClassPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>کلاس آنلاین</CardTitle>
        <CardDescription>کلاس‌های آنلاین شما در این بخش نمایش داده می‌شود.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
