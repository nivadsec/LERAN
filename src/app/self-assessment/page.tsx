'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function SelfAssessmentPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>خودارزیابی</CardTitle>
        <CardDescription>در این بخش می‌توانید خودارزیابی جدیدی انجام دهید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
