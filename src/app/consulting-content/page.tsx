'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ConsultingContentPage() {
  return (
    <Card>
      <CardHeader className="text-right">
        <CardTitle>مطالب مشاوره‌ای</CardTitle>
        <CardDescription>در این بخش می‌توانید مطالب مشاوره‌ای را مشاهده کنید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
