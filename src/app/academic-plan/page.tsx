'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AcademicPlanPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>برنامه تحصیلی</CardTitle>
        <CardDescription>برنامه تحصیلی شما در این بخش نمایش داده می‌شود.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
