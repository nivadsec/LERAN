'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminMessagesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>پیام‌ها</CardTitle>
        <CardDescription>در این بخش می‌توانید پیام‌ها را مشاهده و مدیریت کنید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
