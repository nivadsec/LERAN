'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminQnAPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>پرسش و پاسخ</CardTitle>
        <CardDescription>در این بخش می‌توانید پرسش و پاسخ‌ها را مدیریت کنید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
