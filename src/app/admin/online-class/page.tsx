'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminOnlineClassPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>کلاس آنلاین</CardTitle>
        <CardDescription>در این بخش می‌توانید کلاس‌های آنلاین را مدیریت کنید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
