'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminPermissionsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>دسترسی‌ها</CardTitle>
        <CardDescription>در این بخش می‌توانید دسترسی‌های کاربران را مدیریت کنید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
