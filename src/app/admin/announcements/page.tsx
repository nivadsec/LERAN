'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminAnnouncementsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>اطلاعیه‌ها</CardTitle>
        <CardDescription>در این بخش می‌توانید اطلاعیه‌ها را مدیریت کنید.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>این صفحه در حال ساخت است.</p>
      </CardContent>
    </Card>
  );
}
